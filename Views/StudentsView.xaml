<UserControl x:Class="UniversityMenuApp.Views.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Mantenimiento de Alumnos"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1"
                    Orientation="Vertical"
                    Margin="0,0,0,20"
                    Width="300">

            <TextBlock Text="Name"/>
            <TextBox Text="{Binding FormFullName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5,0,15"/>

            <TextBlock Text="Email"/>
            <TextBox Text="{Binding FormEmail, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5,0,15"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">

                <Button Content="Agregar"
                        Width="90"
                        Command="{Binding AddStudentCommand}" Click="Button_Click_2"/>

                <Button Content="Modificar"
                        Width="90"
                        Margin="0,0,10,0"
                        Command="{Binding UpdateStudentCommand}"/>
                <Button Content="Eliminar"
                        Width="51"
                        Command="{Binding DeleteStudentCommand}"/>
                <Button Content="Exportar"
                        Width="58"
                        Command="{Binding ExportExcelCommand}"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}"
                      
                       Margin="0,5,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Students}"
                  SelectedItem="{Binding SelectedStudent, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  Margin="0,10,0,0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding Id}"
                                    Width="60"/>

                <DataGridTextColumn Header="Name"
                                    Binding="{Binding FullName}"
                                    Width="*"/>

                <DataGridTextColumn Header="Email"
                                    Binding="{Binding Email}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>