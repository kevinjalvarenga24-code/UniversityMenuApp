<UserControl x:Class="UniversityMenuApp.Views.SubjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Mantenimiento Asignatura"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,20"/>
        <StackPanel Grid.Row="1"
                    Orientation="Vertical"
                    Margin="0,0,0,20"
                    Width="300">

            <TextBlock Text="Name" RenderTransformOrigin="0.5,0.5" Width="208">
                <TextBlock.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-0.216"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </TextBlock.RenderTransform>
            </TextBlock>

            <TextBox Text="{Binding formFullSubject, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5,0,15"/>

            <TextBlock Text="Email"/>
            <TextBox Text="{Binding formemail, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,5,0,0"/>

        </StackPanel>
        <DataGrid
                  ItemsSource="{Binding Subjects}"
                  SelectedItem="{Binding selectedSubject, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  Height="250" Margin="-10,113,10,0" Grid.RowSpan="2" Grid.Row="1">

            <DataGrid.Columns>

                <DataGridTextColumn Header="ID"
                                    Binding="{Binding Id}"
                                    Width="60"/>

                <DataGridTextColumn Header="Name"
                                    Binding="{Binding FullSubject}"
                                    Width="*"/>

          



            </DataGrid.Columns>

        </DataGrid>

    </Grid>


</UserControl>