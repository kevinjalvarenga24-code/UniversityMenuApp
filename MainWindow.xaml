<Window x:Class="UniversityMenuApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UniversityMenuApp.ViewModels"
        xmlns:views="clr-namespace:UniversityMenuApp.Views"
        xmlns:controls="clr-namespace:UniversityMenuApp.Controls"
        mc:Ignorable="d"
        Title="University Menu App - DAY 1 (Right panel + Students empty)" Height="600" Width="980"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <views:HomeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StudentsViewModel}">
            <views:StudentsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TeachersViewModel}">
            <views:TeachersView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SubjectsViewModel}">
            <views:SubjectsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F1F1F1" Padding="8">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="MENÚ" FontSize="16" FontWeight="SemiBold" Margin="8,4,8,12"/>
                <ItemsControl ItemsSource="{Binding MenuOptions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="0">
                                <controls:MenuItemControl/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Background="White">
            <ContentControl Content="{Binding CurrentViewModel}" Margin="16"/>
        </Border>
    </Grid>
</Window>
